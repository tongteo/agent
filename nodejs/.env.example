# Gemini Web (Playwright) — cần Chromium CDP (localhost:9222) hoặc Playwright browser
GEMINI_COOKIES=1

# Claude Web (Playwright) — cần Chromium CDP (localhost:9222) hoặc Playwright browser
# CLAUDE_COOKIES=1

# CDP endpoint (mặc định http://localhost:9222)
# CDP_URL=http://localhost:9222

# Tự động chạy lệnh không cần xác nhận
# AUTO_EXEC=true
